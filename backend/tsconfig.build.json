{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "declaration": true,
    "noEmit": false,          // 🔥 חובה – לוודא שכן מוציאים JS
    "emitDeclarationOnly": false, // 🔥 לבטל מצב של d.ts בלבד אם קיים בבסיס
    "noEmitOnError": false,   // 🔥 Emit files even with errors (for decorator compatibility)
    "rootDir": "./src",
    "outDir": "./dist"
  },
  "include": ["src/**/*.ts"],
  "exclude": [
    "node_modules",
    "test",
    "scripts",
    "prisma/seed.ts",
    "vite.config.ts",
    "**/*.spec.ts",
    "**/*.e2e-spec.ts"
  ]
}
