import React from 'react';
import { View, Text } from 'react-native';

// Placeholder stub; returns fallback text. You can replace with SVG + capture to PNG.
export async function createShareCardPNG(_args: { n: number; username: string }): Promise<{ uri?: string; message: string }> {
  return { message: '🔥 Streak on SocialGym! Try to beat me!' };
}


